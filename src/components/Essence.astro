---
// Sección: Nuestra Esencia (Sobre la Marca)
// Fondo: Gris muy suave (#F5F5F5)
// Elemento Visual: Isotipo (hojas) en marca de agua
---

<section class="essence" id="esencia">
  <!-- Marca de agua del isotipo -->
  <div class="watermark" aria-hidden="true">
    <svg viewBox="0 0 200 300" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M100 20C100 20 80 80 80 140C80 200 100 280 100 280C100 280 120 200 120 140C120 80 100 20 100 20Z" 
            fill="none" stroke="rgba(161,160,160,0.08)" stroke-width="3"/>
      <path d="M70 40C70 40 30 100 40 160C50 220 85 270 85 270" 
            fill="none" stroke="rgba(161,160,160,0.06)" stroke-width="3"/>
      <path d="M130 40C130 40 170 100 160 160C150 220 115 270 115 270" 
            fill="none" stroke="rgba(161,160,160,0.06)" stroke-width="3"/>
    </svg>
  </div>

  <div class="essence-container">
    <div class="essence-content">
      <!-- Decorador superior -->
      <div class="decorator">
        <span class="decorator-line"></span>
        <span class="decorator-text">Nuestra Filosofía</span>
        <span class="decorator-line"></span>
      </div>

      <h2 class="essence-title">
        Más que estética, <em>armonía</em>.
      </h2>

      <p class="essence-text">
        Nacimos con la visión de resaltar la belleza natural, expandiendo nuestra 
        experiencia desde Colombia hasta Ecuador. En Bel, creemos en la fluidez 
        de las líneas y la pureza de los resultados.
      </p>

      <p class="essence-highlight">
        No buscamos cambiarte, buscamos <strong>equilibrarte</strong>.
      </p>

      <!-- Valores en grid -->
      <div class="values-grid">
        <div class="value-item">
          <div class="value-icon">
            <svg viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg">
              <circle cx="20" cy="20" r="18" stroke="#606060" stroke-width="1"/>
              <path d="M20 8V20L28 24" stroke="#606060" stroke-width="1.5" stroke-linecap="round"/>
            </svg>
          </div>
          <h3 class="value-title">Experiencia</h3>
          <p class="value-desc">Años de trayectoria en medicina estética</p>
        </div>

        <div class="value-item">
          <div class="value-icon">
            <svg viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M20 5L23 15H33L25 22L28 32L20 25L12 32L15 22L7 15H17L20 5Z" 
                    stroke="#606060" stroke-width="1" fill="none"/>
            </svg>
          </div>
          <h3 class="value-title">Excelencia</h3>
          <p class="value-desc">Tecnología de última generación</p>
        </div>

        <div class="value-item">
          <div class="value-icon">
            <svg viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M20 35C20 35 32 28 32 18C32 11 27 6 20 6C13 6 8 11 8 18C8 28 20 35 20 35Z" 
                    stroke="#606060" stroke-width="1" fill="none"/>
            </svg>
          </div>
          <h3 class="value-title">Cuidado</h3>
          <p class="value-desc">Cada paciente es único y especial</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .essence {
    position: relative;
    background-color: var(--color-gray-soft);
    padding: 8rem 0;
    overflow: hidden;
  }

  /* Marca de agua */
  .watermark {
    position: absolute;
    right: -5%;
    top: 50%;
    transform: translateY(-50%);
    width: 400px;
    height: 600px;
    opacity: 1;
    pointer-events: none;
  }

  .watermark svg {
    width: 100%;
    height: 100%;
  }

  /* Contenedor */
  .essence-container {
    position: relative;
    z-index: 1;
    max-width: 900px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .essence-content {
    text-align: center;
  }

  /* Decorador */
  .decorator {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1.5rem;
    margin-bottom: 2rem;
  }

  .decorator-line {
    width: 60px;
    height: 1px;
    background-color: var(--color-gray-light);
  }

  .decorator-text {
    font-family: var(--font-body);
    font-size: 0.75rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--color-gray-light);
  }

  /* Título */
  .essence-title {
    font-family: var(--font-titles);
    font-size: clamp(2rem, 4vw, 3.5rem);
    font-weight: 600;
    color: var(--color-black);
    margin-bottom: 2rem;
  }

  .essence-title em {
    font-style: italic;
    color: var(--color-gray-medium);
  }

  /* Texto */
  .essence-text {
    font-family: var(--font-body);
    font-size: 1.125rem;
    font-weight: 300;
    line-height: 2;
    color: var(--color-gray-medium);
    max-width: 700px;
    margin: 0 auto 1.5rem;
  }

  .essence-highlight {
    font-family: var(--font-body);
    font-size: 1.25rem;
    font-weight: 400;
    color: var(--color-black);
    margin-bottom: 4rem;
  }

  .essence-highlight strong {
    font-weight: 600;
  }

  /* Grid de valores */
  .values-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 3rem;
    margin-top: 4rem;
  }

  .value-item {
    text-align: center;
  }

  .value-icon {
    width: 50px;
    height: 50px;
    margin: 0 auto 1rem;
  }

  .value-icon svg {
    width: 100%;
    height: 100%;
  }

  .value-title {
    font-family: var(--font-titles);
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--color-black);
    margin-bottom: 0.5rem;
  }

  .value-desc {
    font-family: var(--font-body);
    font-size: 0.875rem;
    font-weight: 300;
    color: var(--color-gray-light);
  }

  /* Responsive */
  @media (max-width: 768px) {
    .essence {
      padding: 5rem 0;
    }

    .watermark {
      width: 250px;
      height: 400px;
      right: -10%;
      opacity: 0.5;
    }

    .values-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .decorator-line {
      width: 30px;
    }

    .essence-text {
      font-size: 1rem;
    }
  }
</style>
